import { TimeCounterDirective } from './time-counter.directive';

describe('TimeCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeCounterDirective();
    expect(directive).toBeTruthy();
  });
});
